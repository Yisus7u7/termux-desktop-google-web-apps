# termux-desktop-google-web-apps
A set for google web apps aplications for termux, and extra (telegram web, facebook)

# Build

```bash
termux-create-package TERMUXBUILD.yaml
```

# Install

```bash
apt install ./<output>.deb
```